@page
@model HotelDemo.Pages.Admin.PropertiesModel
@{
    ViewData["Title"] = "Admin — Properties";
}

<div class="container py-4">
    <h2 class="mb-3">Admin — Properties</h2>

    @if (TempData["ok"] is string ok)
    {
        <div class="alert alert-success">@ok</div>
    }
    @if (TempData["err"] is string err)
    {
        <div class="alert alert-danger">@err</div>
    }

    <div class="row g-4">
        <!-- Create -->
        <div class="col-lg-5">
            <div class="card">
                <div class="card-header">Create new property</div>
                <div class="card-body">
                    <form method="post" asp-page-handler="Add">
                        <div asp-validation-summary="ModelOnly" class="text-danger small mb-2"></div>

                        <div class="mb-2">
                            <label class="form-label">Code</label>
                            <input asp-for="Add.Code" class="form-control form-control-sm" />
                            <span asp-validation-for="Add.Code" class="text-danger small"></span>
                        </div>

                        <div class="mb-2">
                            <label class="form-label">Name</label>
                            <input asp-for="Add.Name" class="form-control form-control-sm" />
                            <span asp-validation-for="Add.Name" class="text-danger small"></span>
                        </div>

                        <div class="mb-2">
                            <label class="form-label">Email</label>
                            <input asp-for="Add.Email" class="form-control form-control-sm" />
                            <span asp-validation-for="Add.Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-2">
                            <label class="form-label">Phone</label>
                            <input asp-for="Add.Phone" class="form-control form-control-sm" />
                        </div>

                        <div class="mb-2">
                            <label class="form-label">Address</label>
                            <input asp-for="Add.Address" class="form-control form-control-sm" />
                        </div>

                        <div class="row g-2">
                            <div class="col-6">
                                <label class="form-label">City</label>
                                <input asp-for="Add.City" class="form-control form-control-sm" />
                            </div>
                            <div class="col-6">
                                <label class="form-label">Postal Code</label>
                                <input asp-for="Add.PostalCode" class="form-control form-control-sm" />
                            </div>
                        </div>

                        <div class="row g-2 mt-1">
                            <div class="col-6">
                                <label class="form-label">Country</label>
                                <input asp-for="Add.CountryCode" class="form-control form-control-sm" maxlength="2" placeholder="GR" />
                                <span asp-validation-for="Add.CountryCode" class="text-danger small"></span>
                            </div>
                            <div class="col-3">
                                <label class="form-label">Check-in</label>
                                <input asp-for="Add.DefaultCheckInTime" class="form-control form-control-sm" type="time" />
                            </div>
                            <div class="col-3">
                                <label class="form-label">Check-out</label>
                                <input asp-for="Add.DefaultCheckOutTime" class="form-control form-control-sm" type="time" />
                            </div>
                        </div>

                        <div class="mt-3">
                            <button class="btn btn-primary btn-sm">Create</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- List -->
        <div class="col-lg-7">
            <div class="card">
                <div class="card-header">Existing properties</div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-sm align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Code</th>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>City</th>
                                    <th>Country</th>
                                    <th>Times</th>
                                    <th class="text-end">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Items.Count == 0)
                                {
                                    <tr><td colspan="7" class="text-center text-muted py-3">No properties</td></tr>
                                }
                                else
                                {
                                    @foreach (var p in Model.Items)
                                    {
                                        <tr>
                                            <td>@p.Code</td>
                                            <td>@p.Name</td>
                                            <td>@p.Email</td>
                                            <td>@p.City</td>
                                            <td>@p.CountryCode</td>
                                            <td>@p.CheckIn.ToString("HH\\:mm") / @p.CheckOut.ToString("HH\\:mm")</td>
                                            <td class="text-end">
                                                <form method="post" asp-page-handler="Delete" class="d-inline" onsubmit="return confirm('Delete @p.Name?');">
                                                    <input type="hidden" name="id" value="@p.Id" />
                                                    <button class="btn btn-sm btn-outline-danger">Delete</button>
                                                </form>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <div class="alert alert-info mt-2 mb-0">
                <small>
                    Σημείωση: Η διαγραφή επιτρέπεται μόνο αν δεν υπάρχουν σχετικές εγγραφές (δωμάτια, τύποι δωματίων, κρατήσεις, επισκέπτες).
                    Σε αντίθετη περίπτωση, θα εμφανιστεί μήνυμα λάθους.
                </small>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
